---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import coffee from "../assets/coffee.jpg";
import bike from "../assets/bike.jpg";
import george from "../assets/george.jpg";
---

<Layout title="About">
  <header class="mb-20">
    <a
      href="/"
      class="back-link group inline-flex items-center gap-2 hover:no-underline text-text-muted hover:text-accent transition-colors"
      aria-label="Back to posts"
    >
      <svg
        class="w-5 h-5 overflow-visible"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        viewBox="0 0 24 24"
      >
        <path
          d="M20 12H4M9 17l-5-5 5-5"
          stroke-linecap="round"
          stroke-linejoin="round"></path>
      </svg>
      <span class="text-sm">back to posts</span>
    </a>
  </header>

  <main>
    <h1 class="text-3xl font-semibold text-text mb-8">About</h1>

    <div class="space-y-6 text-text-muted leading-relaxed">
      <p>
        I'm Vasisht, a software engineer at LinkedIn based in San Francisco. I
        build web applications—mostly frontend work, but I don't shy away from
        APIs, data analysis, or wherever the problem takes me.
      </p>

      <p>
        In high school, I had a computer science teacher who'd explain programs
        by doing a "dry run" on the blackboard—tracing each line, tracking
        variables, no hand-waving. Just methodical walkthroughs until it
        clicked. That stuck with me.
      </p>

      <p>
        This blog is my version of that. I write to prove to myself that I
        understand something. Right now that means JavaScript concepts and the
        occasional LeetCode problem. If you stumble onto it and it helps you
        too, even better.
      </p>

      <p>Outside of code: coffee, biking, and George.</p>
    </div>

    <!-- Interests collage -->
    <section class="mt-12">
      <!-- Mobile: collage layout -->
      <div class="grid grid-cols-2 gap-3 h-[300px] sm:hidden">
        <!-- Left column: 2 stacked photos -->
        <div class="flex flex-col gap-3">
          <div
            class="group relative flex-1 overflow-hidden rounded-lg border border-secondary hover:border-accent transition-colors"
          >
            <Image
              src={coffee}
              alt="Coffee"
              width={400}
              height={300}
              class="w-full h-full object-cover"
            />
            <span
              class="absolute bottom-2 left-2 text-xs text-white/80 opacity-0 group-hover:opacity-100 transition-opacity bg-primary/60 px-1.5 py-0.5 rounded"
            >
              coffee
            </span>
          </div>
          <div
            class="group relative flex-1 overflow-hidden rounded-lg border border-secondary hover:border-accent transition-colors"
          >
            <Image
              src={bike}
              alt="Biking"
              width={400}
              height={300}
              class="w-full h-full object-cover"
            />
            <span
              class="absolute bottom-2 left-2 text-xs text-white/80 opacity-0 group-hover:opacity-100 transition-opacity bg-primary/60 px-1.5 py-0.5 rounded"
            >
              biking
            </span>
          </div>
        </div>
        <!-- Right column: 1 tall photo -->
        <div
          class="group relative overflow-hidden rounded-lg border border-secondary hover:border-accent transition-colors"
        >
          <Image
            src={george}
            alt="George the dog"
            width={400}
            height={600}
            class="w-full h-full object-cover"
          />
          <span
            class="absolute bottom-2 left-2 text-xs text-white/80 opacity-0 group-hover:opacity-100 transition-opacity bg-primary/60 px-1.5 py-0.5 rounded"
          >
            george
          </span>
        </div>
      </div>

      <!-- Desktop: 3-column row -->
      <div class="hidden sm:grid grid-cols-3 gap-3">
        <div
          class="group relative aspect-square overflow-hidden rounded-lg border border-secondary hover:border-accent transition-colors"
        >
          <Image
            src={coffee}
            alt="Coffee"
            width={300}
            height={300}
            class="w-full h-full object-cover"
          />
          <span
            class="absolute bottom-2 left-2 text-xs text-white/80 opacity-0 group-hover:opacity-100 transition-opacity bg-primary/60 px-1.5 py-0.5 rounded"
          >
            coffee
          </span>
        </div>
        <div
          class="group relative aspect-square overflow-hidden rounded-lg border border-secondary hover:border-accent transition-colors"
        >
          <Image
            src={bike}
            alt="Biking"
            width={300}
            height={300}
            class="w-full h-full object-cover"
          />
          <span
            class="absolute bottom-2 left-2 text-xs text-white/80 opacity-0 group-hover:opacity-100 transition-opacity bg-primary/60 px-1.5 py-0.5 rounded"
          >
            biking
          </span>
        </div>
        <div
          class="group relative aspect-square overflow-hidden rounded-lg border border-secondary hover:border-accent transition-colors"
        >
          <Image
            src={george}
            alt="George the dog"
            width={300}
            height={300}
            class="w-full h-full object-cover"
          />
          <span
            class="absolute bottom-2 left-2 text-xs text-white/80 opacity-0 group-hover:opacity-100 transition-opacity bg-primary/60 px-1.5 py-0.5 rounded"
          >
            george
          </span>
        </div>
      </div>
    </section>

    <section class="mt-16">
      <a
        href="https://www.linkedin.com/in/vasishtsankar/"
        target="_blank"
        rel="noopener noreferrer"
        class="text-text-muted hover:text-accent transition-colors text-sm"
      >
        LinkedIn →
      </a>
    </section>
  </main>
</Layout>
